# Management Service Configuration

name = "management_svc"
version = "0.1.0"
port = 8005

[health]
endpoint = "/health"

[discovery]
scan_interval = 30  # seconds
timeout = 5  # seconds

[security]
# Management service uses internal tokens for secure communication
require_internal_auth = true